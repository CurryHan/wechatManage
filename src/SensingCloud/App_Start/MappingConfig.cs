namespace SensingCloud
{
    public class MappingConfig
    {
        public static void RegisterMaps()
        {
            //AutoMapper.Mapper.Initialize(config => {
            //    config.CreateMap<Device, DeviceDetailViewModel>()
            //           .ForMember(dest => dest.DeviceTypeName, opt => opt.MapFrom(src => src.DeviceType.Name))
            //           .ForMember(dest => dest.DeviceGroupName, opt => opt.MapFrom(src => src.DeviceGroup.DisplayName))
            //           .ForMember(dest => dest.Building, opt => opt.MapFrom(src => src.Room != null ? src.Room.Floor.Building.Name : ""))
            //           .ForMember(dest => dest.Floor, opt => opt.MapFrom(src => src.Room != null ? src.Room.Floor.Name : ""))
            //           .ForMember(dest => dest.Room, opt => opt.MapFrom(src => src.Room != null ? src.Room.Name : ""));


            //    config.CreateMap<Device, CreateDeviceViewModel>();

            //    config.CreateMap<ApplicationUser, UserViewModel>().ForMember(dest => dest.Company, opt => opt.MapFrom(src => src.CompanyName));

            //    config.CreateMap<DeviceType, EditDeviceTypeViewModel>().ReverseMap();

            //});

            
        }
    }
}